var layers = selection
if(layers.count() > 0){
	for (var i = 0; i < layers.count(); i++){
		var layer = layers[i];
		var text = [layer stringValue];
		if (text == text.toUpperCase()) {
				[layer setStringValue: text.toLowerCase()];
			} else {
				[layer setStringValue: text.toUpperCase()];
		}
	}
}
